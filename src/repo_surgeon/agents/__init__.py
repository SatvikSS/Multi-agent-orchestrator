"""Agent node implementations for the orchestration graph.

Phase 1 keeps these as pass-through stubs in `nodes.py` so the full graph runs
end-to-end. They gain real LLM/retrieval logic in Phases 2–4 and will be split into
per-agent modules (localizer.py, planner.py, writer.py, critic.py, deliverer.py) as
they grow.
"""
